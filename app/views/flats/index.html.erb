<% @flats.each do |flat| %>
  <div>
    <h2><%=flat.name%></h2>
    <p><%=flat.address%></p>
    <p><%=flat.description%>
      <p>Price: <%= flat.price_per_night%></p>
      <p>Number of guests: <%= flat.number_of_guests%></p>
    </div>
  <% end %>
